general {
    grab_focus = true
}

cache {
    enabled = true
}

finders {
    default_finder = desktop
    desktop_prefix = 
    unicode_prefix = .
    math_prefix = =
    desktop_launch_prefix = uwsm app --
    desktop_icons = false
}

ui {
    window_size = 300 300
}
